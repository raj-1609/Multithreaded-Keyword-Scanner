#ifndef FILE_SEARCH_HPP
#define FILE_SEARCH_HPP
#include <string>
using namespace std;

void search_in_file(const string& rootDir, const string& keyword);
void search_files(const string& rootDir,const string& keyword);

#endif
